apiVersion: v1
kind: Service
metadata:
  name: "{{ include "chatvaultservice.name" . }}"
  labels:
{{ include "chatvaultservice.labels" . | indent 4 }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.service.port }}
  selector:
{{ include "chatvaultservice.matchLabels" . | indent 4 }}